import React, { Component } from 'react'
import { Layout } from '../../wrapper'
const posts = require("../../assets/Posts_content/posts_content.json")

const Post_i = (component) => {
    return (
    <div>
        <Component/>
    </div>
    )
}
    

export default Layout(Post_i)